import { Module } from '@nestjs/common';

/**
 * Empty on purpose. Owns follows, posts, comments, likes, reposts, and the following-based
 * feed starting in Phase 4.
 */
@Module({})
export class SocialModule {}
