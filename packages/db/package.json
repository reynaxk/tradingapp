{
  "name": "@fomo/db",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc -p tsconfig.json",
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:diff": "prisma migrate diff",
    "validate": "prisma validate",
    "studio": "prisma studio",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"no unit tests in @fomo/db — covered by migration verification in CI\" && exit 0",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "@fomo/config": "workspace:*",
    "eslint": "^8.57.1",
    "prisma": "^5.20.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
